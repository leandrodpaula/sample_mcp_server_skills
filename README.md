# Sample MCP Skill Server

A Python-based MCP server using FastMCP to manage user skills via the filesystem.

## Features
- Register skills by user ID
- List skills for a user
- Read skill content

## Configuration
Set `PATH_SKILLS` in `.env` to the desired storage location.
